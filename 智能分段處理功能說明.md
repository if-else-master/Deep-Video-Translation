# Deep Video Translation - 智能分段處理功能

## 📋 功能概述

智能分段處理是 Deep Video Translation 系統的新增功能，能夠自動檢測視頻中的內容變化（人臉/簡報切換），並對不同類型的段落進行對應的深度翻譯處理。

### 🎯 核心特色

- **🔍 智能內容檢測**: 自動識別人臉段落和簡報段落
- **✂️ 精確時間切分**: 基於內容變化進行音視頻同步切分
- **🎭 差異化處理**: 針對不同內容類型採用最適合的翻譯策略
- **🔗 無縫合併**: 處理完成後智能重組，保持原始時序

## 🔧 處理流程

### 1. 內容分段檢測
```
視頻輸入 → 幀採樣分析 → 人臉/簡報檢測 → 內容變化識別 → 段落劃分
```

**參數配置:**
- 最小段落時長: 避免過短片段，建議 2.0 秒
- 內容變化敏感度: 控制切分精度，建議 0.3

### 2. 音視頻切分
```
原始視頻 → FFmpeg精確切分 → 段落視頻文件
原始音頻 → FFmpeg同步切分 → 段落音頻文件
```

### 3. 分段處理策略

#### 👤 人臉段落處理
```
音頻段落 → 語音識別 → 文字翻譯 → 語音克隆 → 嘴形同步 → 處理完成
```

#### 📋 簡報段落處理  
```
視頻段落 → 關鍵幀提取 → OCR文字識別 → 文字翻譯 → 視覺重構
音頻段落 → 語音識別 → 文字翻譯 → 語音克隆 → 音視頻合成
```

#### 🔀 混合段落處理
```
優先嘴形同步 → 失敗則音視頻直接合成
```

### 4. 段落合併
```
所有處理段落 → FFmpeg concat → 最終輸出視頻
```

## 🎬 使用方法

### GUI 操作步驟

1. **啟用功能**: 勾選「啟用智能分段處理」
2. **參數調整**: 
   - 最小段落時長: 2.0秒（推薦）
   - 內容變化敏感度: 0.3（推薦）
3. **基本設置**:
   - 填入 Gemini API Key
   - 選擇語音翻譯語言
   - 選擇投影片翻譯語言
4. **文件選擇**:
   - 輸入視頻: 包含混合內容的視頻文件
   - 輸出路徑: 設置最終結果保存位置
5. **開始處理**: 點擊「開始處理」按鈕

### 程式化調用

```python
from main import DeepVideoTranslationApp

# 創建應用實例
app = DeepVideoTranslationApp(root)

# 啟用智能分段處理
app.segment_processing_var.set(True)

# 設置參數
app.min_segment_duration_var.set("2.0")
app.content_threshold_var.set("0.3")

# 執行處理
app.process_with_segments()
```

## 📊 處理示例

### 視頻結構示例
```
原始視頻: [人臉1] → [簡報1] → [簡報2] → [人臉2] → [簡報3]
          0-10s    10-25s    25-40s    40-55s    55-70s

分段結果: 
- 段落1 (人臉): 0-10s  → 嘴形同步處理
- 段落2 (簡報): 10-25s → OCR翻譯處理  
- 段落3 (簡報): 25-40s → OCR翻譯處理
- 段落4 (人臉): 40-55s → 嘴形同步處理
- 段落5 (簡報): 55-70s → OCR翻譯處理

最終輸出: [翻譯人臉1] → [翻譯簡報1] → [翻譯簡報2] → [翻譯人臉2] → [翻譯簡報3]
```

### 處理結果統計
```
智能分段處理完成！

總段落數: 5
成功處理: 5  
處理失敗: 0

段落詳情:
- 段落1 (face): 語音翻譯 + 嘴形同步 ✅
- 段落2 (slide): 語音翻譯 + OCR翻譯 ✅  
- 段落3 (slide): 語音翻譯 + OCR翻譯 ✅
- 段落4 (face): 語音翻譯 + 嘴形同步 ✅
- 段落5 (slide): 語音翻譯 + OCR翻譯 ✅
```

## ⚙️ 技術細節

### 內容檢測算法

**人臉檢測:**
- 使用 OpenCV Haar Cascade 分類器
- 多參數梯度檢測提高準確率
- 滑動窗口平滑檢測結果

**簡報檢測:**
- 基於邊緣檢測的內容分析
- 排除人臉後的文字內容識別
- 對比度和邊緣比例判斷

**內容分類:**
```python
if face_confidence > threshold:
    content_type = "face"
elif slide_confidence > threshold:  
    content_type = "slide"
else:
    content_type = "mixed"
```

### 切分精度控制

**時間精度**: 基於 FFmpeg 的精確時間切分，支援毫秒級精度
**同步保證**: 音視頻使用相同時間戳確保同步
**邊界處理**: 自動處理段落邊界的平滑過渡

## 🎯 使用建議

### 視頻要求
- **解析度**: 建議 720p 以上
- **人臉大小**: 人臉佔畫面 1/4 以上效果最佳
- **簡報清晰度**: 文字清晰可辨，對比度足夠

### 參數調優
- **短視頻**: 最小段落時長可設為 1.0 秒
- **長視頻**: 建議 3.0 秒以上避免頻繁切換
- **內容複雜**: 敏感度可調高至 0.5
- **內容單純**: 敏感度可調低至 0.2

### 性能最佳化
- **並行處理**: 系統自動使用多核心處理不同段落
- **記憶體管理**: 大視頻會自動分批處理避免記憶體溢出
- **暫存管理**: 處理完成後自動清理暫存檔案

## ❗ 注意事項

### 限制與已知問題
1. **API 配額**: 大量文字翻譯需要足夠的 Gemini API 配額
2. **處理時間**: 複雜視頻的處理時間較長，請耐心等待
3. **格式相容**: 建議使用 MP4 格式確保最佳相容性
4. **硬體需求**: 嘴形同步需要一定的運算資源

### 故障排除

#### 🔍 段落檢測問題
**問題**: 只檢測到一種類型的段落或檢測結果不正確
**解決方案**:
1. 使用調試工具分析視頻: `python debug_segments.py your_video.mp4`
2. 調整內容變化敏感度 (0.1-0.5)
3. 調整最小段落時長 (1.0-5.0秒)
4. 確認視頻確實包含混合內容

#### 🎭 嘴形同步失敗
**問題**: 人臉段落處理失敗
**解決方案**:
1. 檢查人臉是否清晰可見且大小適中
2. 確認視頻質量足夠高
3. 檢查 Wav2Lip 依賴是否正確安裝

#### 📝 OCR 識別失敗
**問題**: 簡報段落的文字翻譯失敗
**解決方案**:
1. 確認簡報文字清晰度和對比度
2. 檢查 easyocr 依賴是否正確安裝
3. 確認 Gemini API 配額充足

#### 🔗 合併失敗
**問題**: 最終視頻只包含部分段落
**解決方案**:
1. 檢查所有段落是否成功處理 (查看詳細日誌)
2. 確認暫存空間充足
3. 檢查 ffmpeg 是否正確安裝
4. 查看段落文件是否存在且不為空

### 📋 調試檢查清單

當遇到問題時，請按順序檢查：

1. **視頻分析**: 
   ```bash
   python debug_segments.py your_video.mp4
   ```

2. **段落檢測**: 查看控制台輸出中的段落檢測結果

3. **文件生成**: 檢查 `temp/segment_XX` 目錄中是否有處理結果

4. **合併過程**: 查看 `temp/segment_list.txt` 文件內容

5. **最終輸出**: 確認輸出文件存在且大小合理

### 🆘 常見錯誤處理

**錯誤**: "沒有成功處理的段落可以合併"
- 檢查每個段落的處理日誌
- 確認 API 配額充足
- 檢查依賴安裝是否完整

**錯誤**: "視頻合併失敗"
- 檢查 ffmpeg 安裝
- 確認段落視頻文件存在
- 嘗試不同的視頻格式

**錯誤**: "段落處理失敗"
- 查看具體錯誤信息
- 檢查對應功能模塊 (語音/視頻/OCR)
- 確認網絡連接正常

## 🔄 更新記錄

- **v1.0.0**: 初始版本，支援基本的智能分段處理
- 計劃更新: 增加更多內容檢測算法，支援更複雜的視頻結構

---

*如需更多技術支援或功能建議，請查看項目文檔或提交 Issue。* 